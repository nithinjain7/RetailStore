name: Build and Push Retail App Images

on:
  push:
    branches:
      - main

env:
  REGISTRY: docker.io
  DB_IMAGE: retail-db
  BACKEND_IMAGE: retail-backend
  FRONTEND_IMAGE: retail-frontend

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    # ---------------- DB ----------------
    - name: Build & Push DB Image
      run: |
        docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.DB_IMAGE }}:latest ./db
        docker push ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.DB_IMAGE }}:latest

    # ---------------- BACKEND ----------------
    - name: Build & Push Backend Image
      run: |
        docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.BACKEND_IMAGE }}:latest ./backend
        docker push ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.BACKEND_IMAGE }}:latest

    # ---------------- FRONTEND ----------------
    - name: Build & Push Frontend Image
      run: |
        docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.FRONTEND_IMAGE }}:latest ./frontend
        docker push ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.FRONTEND_IMAGE }}:latest
